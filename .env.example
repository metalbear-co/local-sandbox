# Integration Tests Configuration Example
# Copy this to .env and customize as needed

# CONTAINER_RUNTIME=docker

# Mirrord binary path (architecture-specific)
# Change this based on your system architecture:
# - aarch64-apple-darwin (Apple Silicon)
MIRRORD_BIN=/Users/name/Projects/metalbear/work/mirrord/target/aarch64-apple-darwin/debug/mirrord

# Namespace for tests
NAMESPACE=test-mirrord

# Operator configuration
OPERATOR_IMAGE=mirrord-operator:custom

# SQS/LocalStack configuration
SQS_QUEUE_URL=http://localhost:4566/000000000000/TestQueue
SQS_REGION=eu-north-1
DEFAULT_TENANT=Avi.Test

# MySQL configuration
MYSQL_HOST=mysql.test-mirrord.svc.cluster.local
MYSQL_DATABASE=testdb

# Kafka configuration
KAFKA_BROKER=kafka:9092
KAFKA_TOPIC=test-topic
KAFKA_TOPIC=test-topic

# AWS test

# QUEUE_NAME="AWS SQS queue name"
# AWS_REGION="AWS region (e.g., us-east-1)""
# AWS_ACCESS_KEY_ID="AWS access key"
# AWS_SECRET_ACCESS_KEY="AWS secret key"
# AWS_ACCOUNT_ID="AWS account ID (for queue URL)""

# GCS_CREDENTIALS_FILE=/path/to/your/gcp-credentials.json
# GCS_BUCKET=your-sccache-bucket
# GCS_PATH_PREFIX=operator-local/  # optional, defaults to "operator-local/"