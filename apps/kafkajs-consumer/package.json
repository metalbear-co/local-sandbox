{
  "name": "kafkajs-consumer",
  "version": "1.0.0",
  "private": true,
  "description": "KafkaJS test consumer reproducing the INCONSISTENT_GROUP_PROTOCOL split failure",
  "main": "consumer.js",
  "dependencies": {
    "kafkajs": "2.2.0"
  }
}
