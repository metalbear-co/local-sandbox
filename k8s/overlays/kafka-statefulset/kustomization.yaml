apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: test-mirrord

resources:
  - ../../base
  - kafka.yaml
  - app.yaml
  - headless-service.yaml
  - kafka-topics-consumer.yaml
