spec:
  template:
    spec:
      containers:
      - name: mirrord-operator
        env:
        - name: GOOGLE_CLOUD_PROJECT
          value: "${GCP_PROJECT}"
        # For service account key auth (non-GKE or without Workload Identity):
        # Mount the key file and set GOOGLE_APPLICATION_CREDENTIALS
        # - name: GOOGLE_APPLICATION_CREDENTIALS
        #   value: "/var/secrets/google/key.json"


