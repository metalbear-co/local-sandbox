apiVersion: v1
kind: ServiceAccount
metadata:
  name: kafka-test-sa
  namespace: test-mirrord
---
# Custom role with minimal permissions needed for Kafka consumer
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kafka-consumer-role
  namespace: test-mirrord
rules:
# Read pods (needed for mirrord to discover targets)
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
# Read services (needed for service discovery)
- apiGroups: [""]
  resources: ["services"]
  verbs: ["get", "list"]
# Read deployments (needed for target resolution)
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch"]
# Read ConfigMaps (if app uses them)
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kafka-test-sa-binding
  namespace: test-mirrord
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kafka-consumer-role
subjects:
- kind: ServiceAccount
  name: kafka-test-sa
  namespace: test-mirrord

