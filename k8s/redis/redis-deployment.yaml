apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-main
  namespace: redis-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-main
  template:
    metadata:
      labels:
        app: redis-main
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-main
  namespace: redis-test
spec:
  selector:
    app: redis-main
  ports:
    - port: 6379
      targetPort: 6379

